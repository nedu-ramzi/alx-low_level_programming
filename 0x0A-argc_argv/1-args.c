#include <stdio.h>
#include "main.h"
/**
  * main - prints the number of argument passed into it
  * @argc: number of argument
  * @argv: character of argument
  * Return: 0
  */

int main(int argc, char *argv[] __attribute__((unused)))
{
	printf("%d\n", argc);

	return (0);
}
